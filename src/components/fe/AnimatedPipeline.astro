---
import { Check, CircleDot } from "lucide-react";

interface Step {
  title: string;
  body: string;
}

interface Props {
  steps?: Step[];
}

const { steps = [] } = Astro.props;
---

<div class="rounded-[var(--radius)] border border-border bg-background p-6 shadow-[var(--shadow-card)]">
  <div class="grid gap-5 md:grid-cols-[1fr_auto_1fr_auto_1fr_auto_1fr]">
    {steps.map((step, index) => (
      <div class="contents">
        <article class:list={["relative min-h-40 rounded-xl border p-5 transition-colors duration-300", index === 0 ? "border-accent/35 bg-accent/5" : "border-border bg-secondary/30"]}>
          <div class="mb-5 flex items-center justify-between gap-3">
            <span class:list={["grid h-9 w-9 place-items-center rounded-full", index === 0 ? "bg-accent text-white" : "bg-stone-200 text-stone-500"]}>
              {index === 0 ? <Check className="h-4 w-4" /> : <CircleDot className="h-4 w-4" />}
            </span>
            <span class="text-xs font-bold uppercase tracking-[0.18em] text-stone-600">{String(index + 1).padStart(2, "0")}</span>
          </div>
          <h3 class="font-heading text-2xl font-semibold text-stone-950">{step.title}</h3>
          <p class="mt-3 text-sm leading-6 text-stone-600">{step.body}</p>
        </article>
        {index < steps.length - 1 && (
          <div class="hidden items-center md:flex" aria-hidden="true">
            <div class="relative h-px w-10 bg-border">
              <div class:list={["absolute inset-y-0 left-0 bg-accent", index === 0 ? "w-full" : "w-0"]}></div>
            </div>
          </div>
        )}
      </div>
    ))}
  </div>
</div>
